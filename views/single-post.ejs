<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head'); %>
<body>
    <%- include('./partials/navbar'); %>
    <div class="container">
        <div class="row valign-wrapper">
            <div class="col s12">
                <h1 style="overflow: hidden"><%= title %></h1>
            </div>
        </div>
        <% if (post) { %>
                <div class="row">                    
                    <div class="card blue-grey darken-1">
                        <div class="card-content white-text">
                            <span class="card-title" style="overflow-x: auto"><%= post.title %></span>
                            <p><%= post.description %></p>
                        </div>
                        <div class="row card-action">
                            <div class="grid-example col s12 m8">
                                <a href="/" class="waves-effect btn purple accent-1">To home</a>
                                <a href="/edit/<%= post._id %>" class="waves-effect btn teal accent-3">Edit</a>
                            </div>
                            <div class="grid-example col s12 m4">
                                <a href="#" class="waves-effect btn red">Date: <%= utils.postTime(post.date) %></a>
                            </div>
                        </div>
                    </div>
                </div>
        <% } else { %>
        <p>No articles!</p>
        <% } %>
    </div>
    <%- include('./partials/footer'); %>
</body>

</html>