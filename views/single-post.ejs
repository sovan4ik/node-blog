<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head'); %>
<body>
    <%- include('./partials/navbar'); %>
    <div class="container">
        <div class="row valign-wrapper">
            <div class="col s12">
                <h1><%= title %></h1>
            </div>
        </div>
        <% if (post) { %>
                <div class="row">                    
                    <div class="card blue-grey darken-1">
                        <div class="card-content white-text">
                            <span class="card-title"><%= post.title %></span>
                            <p><%= post.description %></p>
                        </div>
                        <div class="row card-action">
                            <div class="grid-example col s12 m8">
                                <a href="#" class="waves-effect btn purple accent-1">Open</a>
                                <a href="#" class="waves-effect btn teal accent-3">Rename</a>
                                <form action="/" method="DELETE">
                                    <button type="submit" class="waves-effect btn red delete-post">Delete <%= post._id %></button>
                                </form>
                            </div>
                            <div class="grid-example col s12 m4">
                                <a href="#" class="waves-effect btn red">Date: <%= utils.postTime(post.date) %></a>
                            </div>
                        </div>
                    </div>
                </div>
        <% } else { %>
        <p>No articles!</p>
        <% } %>
    </div>
    <%- include('./partials/footer'); %>
</body>

</html>